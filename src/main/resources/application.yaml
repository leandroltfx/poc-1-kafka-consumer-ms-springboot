spring:
  application:
    name: poc-1-kafka-leitura-pedido-ms-springboot

server:
  port: 8082

config:
  kafka:
    bootstrap-server-config: "localhost:29092"
    topics:
      pedidos-criados: 'poc1.pedidos-criados'
